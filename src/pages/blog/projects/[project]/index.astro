---
import type { WordpressClientIdentifier } from "../../../../types"
import ProjectsListingPage from "../../../../components/projects/ProjectsListingPage.astro";

interface Props {
  project: string
}

export async function getStaticPaths() {
  return [
    {params: { project: 'adamfortuna' }},
    {params: { project: 'hardcover' }},
    {params: { project: 'minafi' }},
  ]
}
const { project } = Astro.params
---

<ProjectsListingPage page={1} project={project as WordpressClientIdentifier} />